server:
    port: 80

spring:
    # H2 Database
    datasource:
        driver-class-name: org.h2.Driver
        url: jdbc:h2:mem:b1a2-s3   # H2 DB ?? ?? (In-Memory Mode)
        username: hanna        # H2 DB ?? ID (??? ??)
        password:               # H2 DB ?? PW (??? ??)

    # H2 Console ??
    h2:
        console: # H2 DB
            enabled: true           # H2 Console ?? ??
            path: /h2-console       # H2 Console ?? ??

    # JPA ??
    jpa:
        database-platform: org.hibernate.dialect.H2Dialect
        hibernate:
            ddl-auto: create        # DB ??? ?? (none, create, create-drop, update, validate)
            properties:
                hibernate:
                    dialect: org.hibernate.dialect.H2Dialect
                    format_sql: true      # ?? ?? ?? (??)
                    show_sql: true        # ?? ?? ??
    servlet:
        multipart:
            max-file-size: 20MB
            max-request-size: 20MB

cloud:
    aws:
        s3:
            bucket: b1a2s3task
        credentials:
            access-key: AKIA5YN5EZY7HJAZG7GA
            secret-key: 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
        region:
            static: ap-northeast-2
            auto: false
    stack:
        auto: false

